(function(){"use strict";const _esc=s=>String(s??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;"),_now=()=>typeof performance<"u"&&performance.now?performance.now():Date.now(),LS_KEY="safetyLab.benchResults.v1";function _mulberry32(seed){let a=seed>>>0;return function(){a|=0,a=a+1831565813|0;let t=Math.imul(a^a>>>15,1|a);return t=t+Math.imul(t^t>>>7,61|t)^t,((t^t>>>14)>>>0)/4294967296}}function benchGenTree(cfg){cfg=cfg||{};const target=Math.max(2,cfg.events|0||100),fanout=Math.max(2,cfg.fanout|0||3),repeatPct=Math.min(.5,Math.max(0,+cfg.repeatPct||0)),rnd=_mulberry32(cfg.seed|0||42);let nextId=9000001;const slots=[];let gates=0;function build(n,gateType){const g={id:nextId++,type:"gate",gateType,probability:0,children:[],name:gateType+"-"+(nextId-9000001)};gates++;const k=Math.max(2,Math.min(n,2+Math.floor(rnd()*(fanout-1)))),parts=new Array(k).fill(1);for(let r=n-k;r>0;r--)parts[Math.floor(rnd()*k)]++;return parts.forEach(p=>{p===1?slots.push(g):g.children.push(build(p,gateType==="OR"?"AND":"OR"))}),g}const root=build(target,"OR"),REPEAT_WINDOW=12,leaves=[];let repeated=0;return slots.forEach(g=>{if(leaves.length>3&&rnd()<repeatPct){const lo=Math.max(0,leaves.length-REPEAT_WINDOW),src=leaves[lo+Math.floor(rnd()*(leaves.length-lo))];g.children.push({id:nextId++,logicalId:src.logicalId,displayId:src.displayId,name:src.name,type:"basic",probability:src.probability,lambda:src.lambda,children:[]}),repeated++}else{const p=1e-6+rnd()*9e-5,id=nextId++,leaf={id,logicalId:"B-"+id,displayId:"BE-"+id,name:"Synthetic event "+id,type:"basic",probability:p,lambda:p,children:[]};leaves.push(leaf),g.children.push(leaf)}}),{root,stats:{leafSlots:slots.length,uniqueEvents:leaves.length,repeated,gates}}}function benchTiers(heavy){const t=[{label:"S",events:100,fanout:3,repeatPct:.1,seed:101},{label:"M",events:250,fanout:3,repeatPct:.1,seed:102},{label:"L",events:500,fanout:3,repeatPct:.12,seed:103},{label:"XL",events:1e3,fanout:4,repeatPct:.12,seed:104},{label:"2K",events:2e3,fanout:4,repeatPct:.15,seed:105}];return heavy&&t.push({label:"5K",events:5e3,fanout:4,repeatPct:.15,seed:106,heavy:!0}),t}function _runTier(cfg){const gen=benchGenTree(cfg),row={label:cfg.label||String(cfg.events),events:cfg.events,uniqueEvents:gen.stats.uniqueEvents,repeated:gen.stats.repeated,gates:gen.stats.gates};let t0=_now();try{const r=computeExactProbability(gen.root);row.pTopMs=+(_now()-t0).toFixed(1),row.pTop=r.prob,row.bddSize=r.bddSize}catch(err){row.pTopMs=+(_now()-t0).toFixed(1),row.pTopError=err&&err.message||String(err)}t0=_now();try{const mcs=getCutsets(gen.root);row.mcsMs=+(_now()-t0).toFixed(1),row.mcsCount=mcs.length}catch(err){row.mcsMs=+(_now()-t0).toFixed(1),err&&err.name==="CutsetExplosionError"?(row.mcsRefused=!0,row.mcsDetail=err.message):err&&(err instanceof RangeError||/call stack/i.test(err.message||""))?(row.mcsOverflow=!0,row.mcsDetail="Enumeration exceeded the browser call stack before the budget guard could fire — cut-set enumeration is infeasible at this scale in one pass. Partition the tree with transfer gates. (Guard hardening is on the engineering plan.)"):row.mcsError=err&&err.message||String(err)}t0=_now();try{const imp=computeImportanceMeasures(gen.root);row.impMs=+(_now()-t0).toFixed(1),row.impCount=imp&&imp.measures&&imp.measures.length||0}catch(err){row.impMs=+(_now()-t0).toFixed(1),row.impError=err&&err.message||String(err)}return row}function benchRun(tiers,onProgress){return tiers=tiers||benchTiers(!1),new Promise(resolve=>{const rows=[];let i=0;function step(){if(i>=tiers.length){const result={at:new Date().toISOString(),ua:typeof navigator<"u"&&navigator.userAgent||"node",rows};try{localStorage.setItem(LS_KEY,JSON.stringify(result))}catch{}resolve(result);return}const cfg=tiers[i++];if(onProgress)try{onProgress(cfg,i-1,tiers.length)}catch{}setTimeout(()=>{rows.push(_runTier(cfg)),step()},30)}step()})}const _ms=v=>v==null?"—":v>=1e3?(v/1e3).toFixed(2)+" s":v+" ms";function _mcsCell(r){return r.mcsRefused?'<span style="color:#9A6200; font-weight:700;" title="'+_esc(r.mcsDetail||"")+'">guard refused ✋</span> <span style="color:var(--color-text-tertiary); font-size:10.5px;">('+_ms(r.mcsMs)+" — refuses, never approximates)</span>":r.mcsOverflow?'<span style="color:#9A6200; font-weight:700;" title="'+_esc(r.mcsDetail||"")+'">overflowed ✋</span> <span style="color:var(--color-text-tertiary); font-size:10.5px;">('+_ms(r.mcsMs)+" — infeasible in one pass; partition with transfer gates)</span>":r.mcsError?'<span style="color:#8E2A2A;" title="'+_esc(r.mcsError)+'">error</span>':"<b>"+_ms(r.mcsMs)+'</b> <span style="color:var(--color-text-tertiary); font-size:10.5px;">('+(r.mcsCount!=null?r.mcsCount.toLocaleString():"—")+" sets)</span>"}function _copyMarkdown(result){const h=`| Tier | Events (unique+rep) | Gates | P(top) exact | BDD nodes | Min cut sets | Importance |
|---|---|---|---|---|---|---|`,lines=result.rows.map(r=>"| "+r.label+" | "+r.uniqueEvents+"+"+r.repeated+" | "+r.gates+" | "+_ms(r.pTopMs)+" | "+(r.bddSize!=null?r.bddSize.toLocaleString():"—")+" | "+(r.mcsRefused?"guard refused ("+_ms(r.mcsMs)+")":r.mcsOverflow?"infeasible in one pass ("+_ms(r.mcsMs)+")":_ms(r.mcsMs)+" ("+(r.mcsCount||0)+" sets)")+" | "+_ms(r.impMs)+" |");return`Safety Lab Aero — engine benchmarks (synthetic structural trees, measured on this machine)
Run: `+result.at+" · "+result.ua+`

`+h+`
`+lines.join(`
`)}function renderBenchPage(){const host=document.getElementById("view-bench");if(!host)return;let last=null;try{last=JSON.parse(localStorage.getItem(LS_KEY)||"null")}catch{}let html='<h3>Scale &amp; Performance <span style="font-size:13px; font-weight:500; color:var(--color-text-tertiary); margin-left:8px;">— the real engine, timed on synthetic structural trees, on THIS machine</span></h3>';html+='<p style="font-size:12px; color:var(--color-text-secondary); ">Each tier generates a deterministic synthetic fault tree (seeded — same seed, same tree, on any machine) and runs the exact functions the app itself calls: BDD-exact P(top), minimal cut sets, and the six importance measures. Trees are structural benchmarks, not aircraft data. When the cut-set explosion guard refuses a tier, that refusal is shown — the engine never approximates past its budget. Timings currently run on the main thread; the page will be busy during a run.</p>',html+='<div style="display:flex; gap:8px; align-items:center; margin:12px 0;"><button class="ckpt-m-btn ckpt-m-btn-primary" style="font-size:12px; padding:5px 16px;" onclick="benchRunUi(false)">Run standard tiers (100 → 2,000 events)</button><button class="ckpt-m-btn" style="font-size:12px; padding:5px 12px;" title="Adds a 5,000-event tier. Expect the page to be unresponsive for the duration of that tier." onclick="benchRunUi(true)">+ heavy tier (5,000)</button>'+(last?'<button class="ckpt-m-btn" style="font-size:12px; padding:5px 12px;" onclick="benchCopyUi()">Copy as Markdown</button>':"")+'<span id="bench-status" class="u-mono" style="font-size:11.5px; color:var(--color-text-secondary);"></span></div>',last&&last.rows&&last.rows.length?(html+='<div class="u-mono" style="font-size:11px; color:var(--color-text-tertiary); margin-bottom:8px;">Last run: '+_esc(String(last.at).replace("T"," ").slice(0,19))+" UTC · "+_esc(String(last.ua).slice(0,90))+"</div>",html+='<table class="data-table" style="width:100%; font-size:12px;"><thead><tr><th>Tier</th><th>Events (unique + repeated)</th><th>Gates</th><th>Exact P(top) via BDD</th><th>BDD nodes</th><th>Minimal cut sets</th><th>Importance (6 measures)</th></tr></thead><tbody>',last.rows.forEach(r=>{html+="<tr><td><b>"+_esc(r.label)+'</b></td><td class="u-mono">'+r.uniqueEvents.toLocaleString()+" + "+r.repeated.toLocaleString()+'</td><td class="u-mono">'+r.gates.toLocaleString()+'</td><td class="u-mono">'+(r.pTopError?'<span style="color:#8E2A2A;" title="'+_esc(r.pTopError)+'">error</span>':"<b>"+_ms(r.pTopMs)+'</b> <span style="color:var(--color-text-tertiary); font-size:10.5px;">P='+(r.pTop!=null?r.pTop.toExponential(3):"—")+"</span>")+'</td><td class="u-mono">'+(r.bddSize!=null?r.bddSize.toLocaleString():"—")+'</td><td class="u-mono">'+_mcsCell(r)+'</td><td class="u-mono">'+(r.impError?'<span style="color:#8E2A2A;" title="'+_esc(r.impError)+'">error</span>':"<b>"+_ms(r.impMs)+"</b>")+"</td></tr>"}),html+="</tbody></table>"):html+='<p style="font-size:12px; color:var(--color-text-tertiary);">No results yet on this machine — run the standard tiers.</p>',html+='<p style="font-size:11px; color:var(--color-text-tertiary); font-family:var(--font-mono); margin-top:12px;">Deterministic generator (mulberry32, fixed seeds) · engine primitives identical to the app’s: computeExactProbability / bddMinimalCutsets / computeImportanceMeasures · results persist locally (machine-specific — never in project data) · cross-check the math itself in Math Validation (25 cited benchmarks).</p>',host.innerHTML=html}window.benchRunUi=function(heavy){const status=document.getElementById("bench-status"),tiers=benchTiers(!!heavy);benchRun(tiers,(cfg,i,n)=>{status&&(status.textContent="running "+cfg.label+" ("+(i+1)+"/"+n+") — "+cfg.events.toLocaleString()+" events…")}).then(()=>{renderBenchPage();try{showToast("Benchmarks complete.","success",2600)}catch{}})},window.benchCopyUi=function(){try{const last=JSON.parse(localStorage.getItem(LS_KEY)||"null");if(!last)return;const md=_copyMarkdown(last);navigator.clipboard&&navigator.clipboard.writeText&&navigator.clipboard.writeText(md);try{showToast("Benchmark table copied as Markdown.","success",2600)}catch{}}catch{}};function _ensurePage(){if(!document.getElementById("view-bench")){const prev=document.getElementById("view-ffs")||document.getElementById("view-budget")||document.getElementById("view-monitors");if(!prev||!prev.parentNode)return!1;const v=document.createElement("div");v.id="view-bench",v.style.display="none",prev.parentNode.insertBefore(v,prev.nextSibling)}return!0}(function(){if(typeof window>"u"||typeof window.switchTab!="function"||window.switchTab._benchWrapped)return;const orig=window.switchTab,wrapped=function(tabId){const r=orig.apply(this,arguments);try{const v=document.getElementById("view-bench");v&&(v.style.display=tabId==="bench"?"block":"none");const s=document.getElementById("snav-bench");s&&s.classList.toggle("snav-active",tabId==="bench"),tabId==="bench"&&renderBenchPage()}catch{}return r};wrapped._benchWrapped=!0,window.switchTab=wrapped})();function _ready(fn){typeof document>"u"||(document.readyState==="loading"?document.addEventListener("DOMContentLoaded",fn):fn())}_ready(function(){let tries=30;const t=setInterval(function(){(_ensurePage()||--tries<=0)&&clearInterval(t)},250)}),typeof window<"u"&&(window.benchGenTree=benchGenTree,window.benchRun=benchRun,window.benchTiers=benchTiers,window.renderBenchPage=renderBenchPage),typeof globalThis<"u"&&(globalThis.benchGenTree=benchGenTree,globalThis.benchRun=benchRun,globalThis.benchTiers=benchTiers)})();
