(function(){"use strict";function _store(){projectConfig.tolDerate||(projectConfig.tolDerate={stacks:[],derate:[]});const r=projectConfig.tolDerate;return Array.isArray(r.stacks)||(r.stacks=[]),Array.isArray(r.derate)||(r.derate=[]),r}function _save(){try{typeof commitSaveChanges=="function"&&commitSaveChanges()}catch{}}function _toast(m,k,t){try{typeof showToast=="function"&&showToast(m,k||"info",t||3e3)}catch{}}function _ask(msg,dflt){return slPrompt(msg,dflt||"")}const _esc=s=>String(s??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");function _access(){return typeof window._ramHasAccess=="function"?window._ramHasAccess():!0}const _chip=(l,v,warn)=>'<div style="height:32px; display:inline-flex; align-items:center; padding:0 12px; border:1px solid var(--color-border-strong); font-family:var(--font-mono); font-size:12px;">'+l+' <b style="margin-left:6px;'+(warn?" color:#B45309;":"")+'">'+v+"</b></div>";function erf(x){const sign=x<0?-1:1;x=Math.abs(x);const t=1/(1+.3275911*x),y=1-((((1.061405429*t-1.453152027)*t+1.421413741)*t-.284496736)*t+.254829592)*t*Math.exp(-x*x);return sign*y}const phi=z=>.5*(1+erf(z/Math.SQRT2));function tolStack(contributors,limits){const nominal=contributors.reduce((a,c)=>a+c.nominal,0),wc=contributors.reduce((a,c)=>a+Math.abs(c.tol),0),sigma=Math.sqrt(contributors.reduce((a,c)=>a+Math.pow(c.tol/3,2),0)),out={nominal,wcLow:nominal-wc,wcHigh:nominal+wc,wc,sigma,rss3:3*sigma};if(limits&&(limits.lower!=null||limits.upper!=null)){let pLow=0,pHigh=0;limits.lower!=null&&sigma>0&&(pLow=phi((limits.lower-nominal)/sigma)),limits.upper!=null&&sigma>0&&(pHigh=1-phi((limits.upper-nominal)/sigma)),out.pExceed=pLow+pHigh,out.wcInside=(limits.lower==null||out.wcLow>=limits.lower)&&(limits.upper==null||out.wcHigh<=limits.upper)}return out}const DERATE_TYPICAL={"Resistor (power)":.5,"Capacitor (voltage)":.6,"Semiconductor (junction temp margin)":.7,"Semiconductor (power)":.5,"Inductor/transformer (current)":.6,"Relay/switch contacts (current)":.5,"Connector contacts (current)":.5,"Wire/cable (current)":.6,"IC (supply voltage)":.8};function derateVerdict(row){const ratio=row.rated>0?row.applied/row.rated:1/0;return{ratio,pass:ratio<=row.guideline,margin:row.guideline-ratio}}async function tolAddStack(){if(!_access())return;const name=await _ask('Stack name (e.g. "Actuator end-play chain" or "Reference voltage chain"):');if(!name||!name.trim())return;const contributors=(await _ask("Contributors, name:nominal:±tol, semicolon-separated:","Housing:10.00:0.05; Bearing:5.00:0.03; Shaft:-14.90:0.04")||"").split(";").map(seg=>{const p=seg.split(":");return{name:(p[0]||"").trim(),nominal:parseFloat(p[1]),tol:Math.abs(parseFloat(p[2]))}}).filter(c=>c.name&&isFinite(c.nominal)&&isFinite(c.tol));if(contributors.length<2){_toast("Need at least two contributors (name:nominal:tol).","warning",3500);return}const lo=parseFloat(await _ask("Lower spec limit on the result (blank = none):","")),hi=parseFloat(await _ask("Upper spec limit on the result (blank = none):",""));_store().stacks.push({id:"TS-"+Date.now(),name:name.trim(),contributors,limits:{lower:isFinite(lo)?lo:null,upper:isFinite(hi)?hi:null}}),_save(),renderRamTolPage()}async function tolDeleteStack(id){const s=_store(),i=s.stacks.findIndex(x=>x.id===id);i<0||await slConfirm("Remove this stack?",{danger:!0,okText:"Remove"})&&(s.stacks.splice(i,1),_save(),renderRamTolPage())}async function derateAdd(){if(!_access())return;const part=await _ask('Part (e.g. "R42, load resistor"):');if(!part||!part.trim())return;const cats=Object.keys(DERATE_TYPICAL),pick=await _ask(`Stress category:
`+cats.map((c,i)=>i+1+". "+c+" (typical ≤ "+DERATE_TYPICAL[c]*100+"%)").join(`
`),"1")||"",idx=parseInt(pick)-1;if(!(idx>=0&&idx<cats.length))return;const rated=parseFloat(await _ask("Rated value (datasheet):","1"));if(!(rated>0))return;const applied=parseFloat(await _ask("Applied (worst-case operating) value:","0.4"));if(!(applied>=0))return;const guideline=parseFloat(await _ask("Guideline fraction. Confirm against YOUR derating standard:",DERATE_TYPICAL[cats[idx]]))||DERATE_TYPICAL[cats[idx]];_store().derate.push({id:"DR-"+Date.now(),part:part.trim(),category:cats[idx],rated,applied,guideline}),_save(),renderRamTolPage()}async function derateDelete(id){const s=_store(),i=s.derate.findIndex(x=>x.id===id);i<0||await slConfirm("Remove this derating row?",{danger:!0,okText:"Remove"})&&(s.derate.splice(i,1),_save(),renderRamTolPage())}function renderRamTolPage(){const host=document.getElementById("ram-tol-host");if(!host)return;if(!_access()){host.innerHTML='<div style="border:1px solid var(--color-border-strong); background:var(--color-surface-2); padding:26px 30px; "><h3 style="margin:0 0 10px; border:none; padding:0;">Tolerance &amp; derating analysis is a Pro+ capability</h3></div>';return}const S=_store(),failsWc=S.stacks.filter(st=>tolStack(st.contributors,st.limits).wcInside===!1).length,failsDr=S.derate.filter(d=>!derateVerdict(d).pass).length;let html='<div style="display:flex; gap:10px; flex-wrap:wrap; margin-bottom:14px;">'+_chip("Tolerance stacks",String(S.stacks.length))+_chip("WC violations",String(failsWc),failsWc>0)+_chip("Derating rows",String(S.derate.length))+_chip("Over guideline",String(failsDr),failsDr>0)+"</div>";html+='<h3>Tolerance accumulation — worst case vs RSS</h3><div style="margin:0 0 10px;"><button class="btn-cyan" onclick="tolAddStack()">+ Tolerance stack</button> <span style="font-size:11px; color:var(--color-text-tertiary); font-family:var(--font-mono);">±tol treated as 3σ (stated assumption) · WC = all limits adverse at once · P(exceed) from the normal CDF</span></div>',S.stacks.length||(html+='<p style="color:var(--color-text-tertiary); font-size:13px;">No stacks yet — dimension chains, voltage dividers, timing budgets all fit.</p>'),S.stacks.forEach(st=>{const r=tolStack(st.contributors,st.limits);html+='<h4 style="font-size:12px; font-family:var(--font-mono); letter-spacing:0.06em; text-transform:uppercase; color:var(--color-text-secondary); margin:12px 0 6px;">'+_esc(st.name)+` <a href="#" style="font-size:11px; text-transform:none;" onclick="tolDeleteStack('`+st.id+`'); return false;">remove</a></h4><table class="data-table" style="width:100%;  font-size:12px;"><thead><tr><th>Contributor</th><th>Nominal</th><th>± tol</th></tr></thead><tbody>`+st.contributors.map(c=>"<tr><td>"+_esc(c.name)+'</td><td class="u-mono">'+c.nominal+'</td><td class="u-mono">'+c.tol+"</td></tr>").join("")+'</tbody></table><div style="display:flex; gap:10px; flex-wrap:wrap; margin:8px 0;">'+_chip("Nominal",r.nominal.toFixed(4))+_chip("Worst case",r.wcLow.toFixed(4)+" … "+r.wcHigh.toFixed(4),r.wcInside===!1)+_chip("RSS ±3σ","±"+r.rss3.toFixed(4))+(r.pExceed!=null?_chip("P(exceed limits)",r.pExceed<1e-6?r.pExceed.toExponential(2):(r.pExceed*100).toFixed(4)+"%",r.pExceed>.001):"")+"</div>"+(r.wcInside===!1?'<p style="font-size:12px; color:#8E2A2A; font-family:var(--font-mono);">WORST CASE VIOLATES the spec limits — statistically rare is not the same as impossible; disposition required (tighten tolerances, widen limits, or accept with the RSS probability recorded).</p>':"")}),html+=`<h3 style="margin-top:var(--s-5);">Derating audit — applied vs rated stress</h3><div style="margin:0 0 10px;"><button class="btn-cyan" onclick="derateAdd()">+ Derating row</button> <span style="font-size:11px; color:var(--color-text-tertiary); font-family:var(--font-mono);">guidelines shown are TYPICAL practice — confirm each against the program's derating standard</span></div>`,html+='<table class="data-table" style="width:100%;  font-size:12.5px;"><thead><tr><th>Part</th><th>Category</th><th>Rated</th><th>Applied</th><th>Ratio</th><th>Guideline</th><th>Verdict</th><th></th></tr></thead><tbody>',S.derate.length||(html+='<tr><td colspan="8" style="color:var(--color-text-tertiary);">No derating rows yet.</td></tr>'),S.derate.forEach(d=>{const v=derateVerdict(d);html+="<tr><td>"+_esc(d.part)+'</td><td style="font-size:11.5px;">'+_esc(d.category)+'</td><td class="u-mono">'+d.rated+'</td><td class="u-mono">'+d.applied+'</td><td class="u-mono"'+(v.pass?"":' style="color:#8E2A2A;"')+">"+(v.ratio*100).toFixed(1)+'%</td><td class="u-mono">≤ '+(d.guideline*100).toFixed(0)+"%</td><td>"+(v.pass?'<span style="color:#1D9E75; font-family:var(--font-mono); font-size:11px;">✓ within</span>':'<span style="color:#8E2A2A; font-family:var(--font-mono); font-size:11px;">OVER — reduce stress or justify</span>')+`</td><td><button class="node-delete-btn-inner" style="font-size:11px;" onclick="derateDelete('`+d.id+`')">✕</button></td></tr>`}),html+='</tbody></table><p style="font-size:11px; color:var(--color-text-tertiary); font-family:var(--font-mono); margin-top:12px;">Derating buys failure-rate margin before any statistics happen (NASA RAM Training ch.4); the stress ratios here should agree with the π factors used in the prediction library.</p>',host.innerHTML=html}(function(){if(typeof window.switchTab!="function"||window.switchTab._tolWrapped)return;const orig=window.switchTab,wrapped=function(tabId){const r=orig.apply(this,arguments);try{const v=document.getElementById("view-ram-tol");v&&(v.style.display=tabId==="ram-tol"?"block":"none");const s=document.getElementById("snav-ram-tol");s&&s.classList.toggle("snav-active",tabId==="ram-tol"),tabId==="ram-tol"&&renderRamTolPage()}catch{}return r};wrapped._tolWrapped=!0,window.switchTab=wrapped})(),window.renderRamTolPage=renderRamTolPage,window.tolAddStack=tolAddStack,window.tolDeleteStack=tolDeleteStack,window.derateAdd=derateAdd,window.derateDelete=derateDelete,window._tolStack=tolStack,window._tolErf=erf,window._deratVerdict=derateVerdict})();
