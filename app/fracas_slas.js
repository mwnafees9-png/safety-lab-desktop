(function(){"use strict";const _esc=s=>String(s??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");function _ram(){return typeof projectConfig<"u"&&projectConfig&&projectConfig.ram?projectConfig.ram:null}const _save=()=>{try{typeof commitSaveChanges=="function"?commitSaveChanges():typeof scheduleAutosave=="function"&&scheduleAutosave()}catch{}},_toast=(m,k)=>{try{typeof showToast=="function"&&showToast(m,k||"info",5200)}catch{}},HOUSE_DEFAULTS={triageDays:3,closeDays:{Critical:30,High:60,Medium:90,Low:180},adoptedBy:null,adoptedAt:null};function slaPolicy(){const r=_ram(),p=r&&r.slaPolicy?r.slaPolicy:null;return p?Object.assign({house:!p.adoptedBy},JSON.parse(JSON.stringify(HOUSE_DEFAULTS)),JSON.parse(JSON.stringify(p))):Object.assign({house:!0},JSON.parse(JSON.stringify(HOUSE_DEFAULTS)))}function setSlaPolicy(patch,by){const r=_ram();if(!r)return _toast("No R&M store yet — the SLA policy lives on the program.","error"),!1;if(!(by||"").trim())return _toast("Adopting or editing the SLA policy needs a name — a process owner, not a ghost.","error"),!1;const cur=r.slaPolicy||JSON.parse(JSON.stringify(HOUSE_DEFAULTS));if(patch&&patch.triageDays!=null){const v=+patch.triageDays;if(!(v>=1&&v<=60))return _toast("Triage SLA must be 1–60 days.","error"),!1;cur.triageDays=v}if(patch&&patch.closeDays){for(const sev of["Critical","High","Medium","Low"])if(patch.closeDays[sev]!=null){const v=+patch.closeDays[sev];if(!(v>=1&&v<=730))return _toast("Closure SLA ("+sev+") must be 1–730 days.","error"),!1;(cur.closeDays=cur.closeDays||{})[sev]=v}}return cur.adoptedBy=String(by).trim(),cur.adoptedAt=patch&&patch.at||new Date().toISOString().slice(0,10),r.slaPolicy=cur,_save(),!0}const _days=(a,b)=>Math.floor((Date.parse(b)-Date.parse(a))/864e5);function slaFor(inc,today,policy){const p=policy||slaPolicy(),age=_days(inc.date,today),closed=inc.status==="closed",closeAt=closed&&inc.verifiedAt||today,cycleDays=_days(inc.date,closeAt),limit=p.closeDays&&p.closeDays[inc.severity]||90,triaged=!!(inc.triage&&inc.triage.by),out={ageDays:age,cycleDays,closed,limitDays:limit,triage:triaged?"triaged":age>p.triageDays?"OVERDUE":"due",closure:closed?cycleDays<=limit?"closed-on-time":"closed-late":age>limit?"OVERDUE":age>limit*.8?"due-soon":"on-time",targetMissed:!!(inc.targetDate&&!closed&&today>inc.targetDate)};return out.exception=out.triage==="OVERDUE"||out.closure==="OVERDUE"||out.closure==="closed-late"||out.targetMissed,out}function setTriage(recId,incId,t){const r=_ram(),f=r&&(r.field||[]).find(x=>x&&x.id===recId),inc=f&&(f.incidents||[]).find(x=>x.id===incId);return inc?(t=t||{},typeof t.relevant!="boolean"?(_toast("Triage answers the question: safety-relevant, yes or no.","error"),!1):(t.by||"").trim()?t.relevant===!1&&String(t.rationale||"").trim().length<10?(_toast('NOT safety-relevant is a safety DECISION — it demands a rationale (≥10 chars). "Obviously not" is not a rationale.',"error"),!1):(inc.triage={relevant:t.relevant,rationale:String(t.rationale||"").trim()||null,linkedFcId:String(t.linkedFcId||"").trim()||null,by:String(t.by).trim(),at:t.at||new Date().toISOString().slice(0,10)},_save(),!0):(_toast("Triage needs a name — it is a safety decision, not a checkbox.","error"),!1)):(_toast("fracas: incident not found","error"),!1)}function clearTriage(recId,incId){const r=_ram(),f=r&&(r.field||[]).find(x=>x&&x.id===recId),inc=f&&(f.incidents||[]).find(x=>x.id===incId);return!inc||!inc.triage?!1:(delete inc.triage,_save(),!0)}function prSeed(rec,inc){return{title:"FRACAS "+inc.id+" — "+inc.mode+" on "+(rec.name||rec.itemId||rec.id),safetyRelated:!0,source:"FRACAS incident "+inc.id+" ("+inc.date+")",linked:inc.triage&&inc.triage.linkedFcId||"",note:"Seed from the FRACAS triage lane — raise it in the Problem Reports register; nothing is auto-written."}}function _allIncidents(){const r=_ram(),out=[];return(r&&r.field||[]).forEach(f=>(f.incidents||[]).forEach(i=>out.push({rec:f,inc:i}))),out}function trend(today,monthsBack){const N=monthsBack||6,p=slaPolicy(),all=_allIncidents(),ym=d=>String(d).slice(0,7),months=[],t=new Date(Date.parse(String(today).slice(0,10)+"T00:00:00Z"));for(let k=N-1;k>=0;k--){const d=new Date(Date.UTC(t.getUTCFullYear(),t.getUTCMonth()-k,1));months.push(d.toISOString().slice(0,7))}return months.map(m=>{const monthEnd=m+"-28",opened=all.filter(x=>ym(x.inc.date)===m),closed=all.filter(x=>x.inc.status==="closed"&&x.inc.verifiedAt&&ym(x.inc.verifiedAt)===m),openAtEnd=all.filter(x=>x.inc.date<=monthEnd&&!(x.inc.status==="closed"&&x.inc.verifiedAt&&x.inc.verifiedAt<=monthEnd)),overdue=openAtEnd.filter(x=>slaFor(x.inc,monthEnd,p).closure==="OVERDUE"),mttc=closed.length?closed.reduce((a,x)=>a+_days(x.inc.date,x.inc.verifiedAt),0)/closed.length:null,cohort=all.filter(x=>x.inc.date<=monthEnd),triaged=cohort.filter(x=>x.inc.triage&&x.inc.triage.by),modes={};cohort.forEach(x=>{const k2=(x.rec.id||"")+"§"+x.inc.mode;modes[k2]=(modes[k2]||0)+1});const repeats=Object.values(modes).filter(n=>n>=2).length;return{month:m,opened:opened.length,closed:closed.length,openAtEnd:openAtEnd.length,overdueAtEnd:overdue.length,mttcDays:mttc==null?null:Math.round(mttc),triagedPct:cohort.length?Math.round(100*triaged.length/cohort.length):null,repeatModes:repeats}})}typeof invRegister=="function"&&invRegister({id:"INV-41",sev:"advisory",name:"FRACAS process discipline — triage on the clock, safety-relevant incidents linked, closure SLAs named when overdue",run:function(){try{const all=_allIncidents();if(!all.length)return{checked:0,fails:[]};const today=new Date().toISOString().slice(0,10),p=slaPolicy(),fails=[];return all.forEach(x=>{const v=slaFor(x.inc,today,p);v.triage==="OVERDUE"&&fails.push(x.inc.id+" ("+(x.rec.name||x.rec.id)+"): UNTRIAGED "+v.ageDays+" days — the safety question has not been asked (SLA "+p.triageDays+"d)"),x.inc.triage&&x.inc.triage.relevant===!0&&!x.inc.triage.linkedFcId&&fails.push(x.inc.id+": safety-RELEVANT but linked to no failure condition and no problem report — the finding is floating"),v.closure==="OVERDUE"&&fails.push(x.inc.id+" ("+x.inc.severity+"): open "+v.ageDays+"d against a "+v.limitDays+"d closure SLA")}),{checked:all.length,fails}}catch{return{checked:0,fails:[]}}}});const _bar=(v,max)=>{const n=max>0?Math.round(6*v/max):0;return"▁▂▃▄▅▆▇".slice(n,n+1)||"▁"};function renderSlaPanel(){if(typeof document>"u")return;const host=document.getElementById("ram-rel-host");if(!host)return;let card=document.getElementById("fracas-sla-card");const r=_ram(),all=_allIncidents();if(!r||!all.length){card&&card.remove();return}card||(card=document.createElement("div"),card.id="fracas-sla-card",host.appendChild(card));const today=new Date().toISOString().slice(0,10),p=slaPolicy(),exceptions=all.map(x=>({x,v:slaFor(x.inc,today,p)})).filter(y=>y.v.exception||!y.x.inc.triage&&y.v.triage!=="triaged"),tr=trend(today,6),maxOpen=Math.max(1,...tr.map(m=>m.openAtEnd));card.innerHTML='<div style="border:1px solid var(--color-border-strong); background:var(--color-surface-1); margin-top:14px;"><div style="padding:9px 14px; border-bottom:2px solid var(--color-text-primary); display:flex; justify-content:space-between; gap:10px; align-items:center;"><b>FRACAS timeliness &amp; triage — the process layer</b><span class="u-mono" style="font-size:10px; color:'+(p.house?"#B7791F":"var(--color-text-tertiary)")+';">'+(p.house?"HOUSE-DEFAULT SLAs — adopt them below to make them yours":"policy adopted by "+_esc(p.adoptedBy)+" · "+_esc(p.adoptedAt))+'</span></div><div style="padding:10px 14px;"><div style="font-size:11px; color:var(--color-text-secondary); margin-bottom:8px;">Triage SLA '+p.triageDays+"d · closure SLAs (days): "+["Critical","High","Medium","Low"].map(s=>s+" "+p.closeDays[s]).join(" · ")+"</div>"+(exceptions.length?'<table class="data-table" style="width:100%; font-size:11px;"><thead><tr><th>Incident</th><th>Record</th><th>Sev</th><th>Age</th><th>Triage</th><th>Closure</th><th>Target</th><th>Safety triage</th></tr></thead><tbody>'+exceptions.map(y=>'<tr><td class="u-mono">'+_esc(y.x.inc.id)+"</td><td>"+_esc(y.x.rec.name||y.x.rec.id)+"</td><td>"+_esc(y.x.inc.severity)+'</td><td class="u-mono">'+y.v.ageDays+"d</td><td>"+(y.v.triage==="OVERDUE"?'<b style="color:#B91C1C;">UNTRIAGED (SLA blown)</b>':_esc(y.v.triage))+"</td><td>"+(/OVERDUE|late/.test(y.v.closure)?'<b style="color:#B91C1C;">'+_esc(y.v.closure)+"</b>":_esc(y.v.closure))+"</td><td>"+(y.v.targetMissed?'<b style="color:#B7791F;">missed</b>':"—")+"</td><td>"+(y.x.inc.triage?y.x.inc.triage.relevant?'<span style="color:#B91C1C; font-weight:700;">SAFETY-RELEVANT</span>'+(y.x.inc.triage.linkedFcId?" → "+_esc(y.x.inc.triage.linkedFcId):' <b style="color:#B7791F;">(unlinked!)</b>'):"not relevant — "+_esc((y.x.inc.triage.rationale||"").slice(0,40)):`<button class="ckpt-m-btn" style="font-size:10px;" onclick="FRACAS_SLA.uiTriage('`+_esc(y.x.rec.id)+"','"+_esc(y.x.inc.id)+`')">triage…</button>`)+"</td></tr>").join("")+"</tbody></table>":'<div style="font-size:11.5px; color:#1D6E3E;">✓ No exceptions — every incident triaged on the clock, every closure inside its SLA.</div>')+'<table class="data-table" style="width:100%; font-size:10.5px; margin-top:10px;"><thead><tr><th>Month</th><th>Opened</th><th>Closed</th><th>Open @ end</th><th>Overdue</th><th>MTTC (d)</th><th>Triaged %</th><th>Repeat modes</th></tr></thead><tbody>'+tr.map(m=>'<tr><td class="u-mono">'+m.month+"</td><td>"+m.opened+"</td><td>"+m.closed+'</td><td class="u-mono">'+_bar(m.openAtEnd,maxOpen)+" "+m.openAtEnd+"</td><td>"+(m.overdueAtEnd?'<b style="color:#B91C1C;">'+m.overdueAtEnd+"</b>":"0")+"</td><td>"+(m.mttcDays==null?"—":m.mttcDays)+"</td><td>"+(m.triagedPct==null?"—":m.triagedPct+"%")+"</td><td>"+(m.repeatModes?'<b style="color:#B7791F;">'+m.repeatModes+"</b>":"0")+"</td></tr>").join("")+'</tbody></table><div style="display:flex; gap:8px; align-items:center; margin-top:10px; font-size:11px; flex-wrap:wrap;"><span style="color:var(--color-text-tertiary);">SLA policy:</span>triage <input id="fsla-t" type="number" min="1" max="60" value="'+p.triageDays+'" style="width:52px;">d'+["Critical","High","Medium","Low"].map(s=>" · "+s+' <input id="fsla-'+s+'" type="number" min="1" max="730" value="'+p.closeDays[s]+'" style="width:58px;">d').join("")+' <input id="fsla-by" placeholder="adopted by (name)" style="width:150px;"> <button class="ckpt-m-btn" style="font-size:10.5px;" onclick="FRACAS_SLA.uiAdopt()">adopt / update policy</button></div><div style="font-size:10px; color:var(--color-text-tertiary); margin-top:6px;">Exceptions only — the compliant majority stays quiet. KPIs are computed from the ledger, never stored. The Thread Integrity sweep watches the same three disciplines (overdue triage, overdue closure, repeat modes).</div></div></div>'}async function uiTriage(recId,incId){const rel=await slConfirm("Safety triage for "+incId+`:

OK = SAFETY-RELEVANT (you will be asked for the FC id)
Cancel = not safety-relevant (you will be asked for the rationale)`,{title:"Safety triage"}),by=await slPrompt("Triage signature (name):","");if(!(!by||!by.trim()))if(rel){const fc=await slPrompt("Failure-condition id this incident touches (e.g. FC-012). Leave empty to link later (it will flag as unlinked):","")||"";setTriage(recId,incId,{relevant:!0,linkedFcId:fc,by})&&renderSlaPanel()}else{const why=await slPrompt("Rationale: WHY is this not safety-relevant? (≥10 chars, this is a safety decision):","")||"";setTriage(recId,incId,{relevant:!1,rationale:why,by})&&renderSlaPanel()}}function uiAdopt(){const g=id=>{const el=document.getElementById(id);return el?el.value:""};setSlaPolicy({triageDays:g("fsla-t"),closeDays:{Critical:g("fsla-Critical"),High:g("fsla-High"),Medium:g("fsla-Medium"),Low:g("fsla-Low")}},g("fsla-by"))&&(_toast("SLA policy adopted — the clocks now run against your numbers.","success"),renderSlaPanel())}(function(){if(typeof window>"u")return;const tryWrap=function(){if(!window.FRACAS_LEDGER||typeof window.FRACAS_LEDGER.renderLedger!="function"||window.FRACAS_LEDGER.renderLedger._slaWrapped)return!1;const orig=window.FRACAS_LEDGER.renderLedger,wrapped=function(){const r=orig.apply(this,arguments);try{renderSlaPanel()}catch{}return r};return wrapped._slaWrapped=!0,window.FRACAS_LEDGER.renderLedger=wrapped,!0};if(!tryWrap()){let n=0;const iv=setInterval(function(){(tryWrap()||++n>=40)&&clearInterval(iv)},250)}})();const API={HOUSE_DEFAULTS,slaPolicy,setSlaPolicy,slaFor,setTriage,clearTriage,prSeed,trend,renderSlaPanel,uiTriage,uiAdopt};typeof window<"u"&&(window.FRACAS_SLA=API),typeof module<"u"&&(module.exports=API)})();
