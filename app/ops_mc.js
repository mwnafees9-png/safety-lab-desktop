(function(){"use strict";function _rng(seed){let a=seed>>>0;return function(){a|=0,a=a+1831565813|0;let t=Math.imul(a^a>>>15,1|a);return t=t+Math.imul(t^t>>>7,61|t)^t,((t^t>>>14)>>>0)/4294967296}}function _pct(sorted,q){const n=sorted.length;return sorted[Math.min(n-1,Math.floor(n*q))]}function simulate(rec,opts){if(opts=opts||{},!rec)throw new Error("OPS-MC: no field record given. Pick a record from the reliability table.");const incs=Array.isArray(rec.incidents)?rec.incidents:[];if(!incs.length)throw new Error("OPS-MC refused: "+(rec.id||"record")+" has no incidents in the ledger. An operational forecast with no operational history is fiction — author the incidents first.");if(!(rec.hours>0))throw new Error("OPS-MC refused: accrued hours must be > 0 — a rate needs a denominator.");const repairs=incs.filter(i=>i.repairHrs!=null&&i.repairHrs>=0).map(i=>i.repairHrs);if(repairs.length<2)throw new Error("OPS-MC refused: fewer than two recorded repair times on "+(rec.id||"record")+". An empirical bootstrap needs at least two observations — record repairHrs on the incidents.");const horizon=opts.horizonHrs!=null?Number(opts.horizonHrs):1e3;if(!(horizon>0))throw new Error("OPS-MC refused: horizon must be > 0 operating hours.");const N=opts.N!=null?Math.floor(Number(opts.N)):5e3;if(!(N>=100))throw new Error("OPS-MC refused: fewer than 100 trials is a vibe, not a distribution. N ≥ 100.");const target=opts.availTargetPct!=null?Number(opts.availTargetPct):99;if(!(target>0&&target<100))throw new Error("OPS-MC refused: availability target must be inside (0, 100) percent.");const seed=(opts.seed==null?42:opts.seed)>>>0,rng=_rng(seed),lambda=incs.length/rec.hours,fails=new Array(N),down=new Array(N),avail=new Array(N);for(let t=0;t<N;t++){let up=0,nf=0,dt=0;for(;;){const u=Math.max(rng(),1e-12),gap=-Math.log(u)/lambda;if(up+gap>=horizon)break;up+=gap,nf++,dt+=repairs[Math.floor(rng()*repairs.length)]}fails[t]=nf,down[t]=dt,avail[t]=horizon/(horizon+dt)*100}const fs=fails.slice().sort((a,b)=>a-b),ds=down.slice().sort((a,b)=>a-b),as=avail.slice().sort((a,b)=>a-b),below=avail.reduce((a,v)=>a+(v<target?1:0),0),mean=arr=>arr.reduce((a,b)=>a+b,0)/arr.length;return{seed,N,horizonHrs:horizon,lambdaFed:lambda,repairsObserved:repairs.length,availTargetPct:target,failures:{mean:mean(fails),p50:_pct(fs,.5),p90:_pct(fs,.9),p95:_pct(fs,.95),p99:_pct(fs,.99)},downtimeHrs:{mean:mean(down),p50:_pct(ds,.5),p90:_pct(ds,.9),p95:_pct(ds,.95),p99:_pct(ds,.99)},availabilityPct:{p50:_pct(as,.5),p05:_pct(as,.05)},pAvailBelowTargetPct:below/N*100,basis:"Inter-arrivals Exp(λ fed = incidents/hours); repairs bootstrapped from the ledger’s own "+repairs.length+" recorded repair times; availability = horizon/(horizon+downtime). Seeded mulberry32("+seed+"), N = "+N+". Computed, never stored. A forecast, not a demonstration — claimed-lane; credit only what the fleet demonstrates."}}function _esc(s){return String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c])}function _toast(msg,kind,ms){try{typeof window.showToast=="function"&&window.showToast(msg,kind||"info",ms||4600)}catch{}}function _ram(){try{return typeof projectConfig<"u"&&projectConfig&&projectConfig.ram||null}catch{return null}}const IN='style="font:inherit; font-size:11px; padding:3px 5px; margin:0; width:74px; border:1px solid var(--color-border-strong); background:var(--color-surface-2); color:inherit; border-radius:4px;"';function renderCard(){if(typeof document>"u")return;const host=document.getElementById("ram-rel-host");if(!host)return;let card=document.getElementById("om-card");const ram=_ram(),eligible=ram?(ram.field||[]).filter(f=>Array.isArray(f.incidents)&&f.incidents.length):[];if(!eligible.length){card&&card.remove();return}card||(card=document.createElement("div"),card.id="om-card",host.appendChild(card));const prev=card._omResult||null;card.innerHTML='<div style="border:1px solid var(--color-border-strong); background:var(--color-surface-1); margin-top:18px;"><div style="padding:9px 14px; border-bottom:2px solid var(--color-text-primary); display:flex; justify-content:space-between; gap:10px; align-items:center;"><b>Operational forecast — Monte Carlo fed by the ledger</b><span class="u-mono" style="font-size:10.5px; color:var(--color-text-tertiary);">a forecast, not a demonstration — credit only what the fleet demonstrates</span></div><div style="padding:10px 14px; display:flex; gap:10px; align-items:end; flex-wrap:wrap;"><label class="u-mono" style="font-size:10px;">record<br><select id="om-rec" '+IN.replace("width:74px","width:130px")+">"+eligible.map(f=>'<option value="'+_esc(f.id)+'">'+_esc(f.id)+" · "+f.incidents.length+" inc</option>").join("")+'</select></label><label class="u-mono" style="font-size:10px;">horizon (op hrs)<br><input id="om-h" '+IN+' value="1000"></label><label class="u-mono" style="font-size:10px;">trials N<br><input id="om-n" '+IN+' value="5000"></label><label class="u-mono" style="font-size:10px;">seed<br><input id="om-s" '+IN+' value="42"></label><label class="u-mono" style="font-size:10px;">avail target %<br><input id="om-t" '+IN+' value="99"></label><button onclick="OPS_MC.run()" style="font:inherit; font-size:11px; padding:5px 14px; border:1px solid var(--color-text-primary); background:var(--color-text-primary); color:var(--color-surface-1); border-radius:4px; cursor:pointer;">Run forecast</button></div><div id="om-out" style="padding:0 14px 12px;">'+(prev||"")+"</div></div>"}function run(){const ram=_ram(),recId=(document.getElementById("om-rec")||{}).value,rec=ram&&(ram.field||[]).find(f=>f.id===recId),val=id=>(document.getElementById(id)||{}).value;let r;try{r=simulate(rec,{horizonHrs:val("om-h"),N:val("om-n"),seed:parseInt(val("om-s"),10)||0,availTargetPct:val("om-t")})}catch(e){_toast(e.message,"error",6200);return}const f1=v=>Number(v).toFixed(1),html='<div class="u-mono" style="font-size:11px; border:1px solid var(--color-border); border-radius:4px; padding:9px 12px; margin-top:2px;"><b>'+_esc(recId)+"</b> · next "+r.horizonHrs.toLocaleString()+" op hrs · λ fed = "+r.lambdaFed.toExponential(3)+"/hr<br>failures&nbsp;&nbsp;: P50 "+r.failures.p50+" · P90 "+r.failures.p90+" · P95 "+r.failures.p95+" · P99 "+r.failures.p99+" (mean "+f1(r.failures.mean)+")<br>downtime&nbsp;&nbsp;: P50 "+f1(r.downtimeHrs.p50)+" · P90 "+f1(r.downtimeHrs.p90)+" · P95 "+f1(r.downtimeHrs.p95)+" · P99 "+f1(r.downtimeHrs.p99)+" hrs<br>availability: P50 "+r.availabilityPct.p50.toFixed(3)+"% · P05 "+r.availabilityPct.p05.toFixed(3)+"% · P(A &lt; "+r.availTargetPct+"%) = <b>"+r.pAvailBelowTargetPct.toFixed(1)+'%</b><br><span style="color:var(--color-text-tertiary);">'+_esc(r.basis)+"</span></div>",out=document.getElementById("om-out");if(out){out.innerHTML=html;const card=document.getElementById("om-card");card&&(card._omResult=html)}}const API={simulate,renderCard,run,_pct};typeof window<"u"&&typeof document<"u"&&(function wire(){if(typeof window.renderRamRelPage=="function"&&!window.renderRamRelPage._omWrapped){const orig=window.renderRamRelPage,wrapped=function(){const r=orig.apply(this,arguments);try{setTimeout(renderCard,0)}catch{}return r};wrapped._omWrapped=!0,window.renderRamRelPage=wrapped}else!window.renderRamRelPage&&typeof window.addEventListener=="function"&&window.addEventListener("DOMContentLoaded",()=>setTimeout(wire,700))})(),typeof window<"u"&&(window.OPS_MC=API),typeof module<"u"&&(module.exports=API)})();
